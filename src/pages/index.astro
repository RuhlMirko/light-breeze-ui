---
<<<<<<< HEAD
import "../styles/global.css";
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Light Breeze UI - Frutiger Aero CSS Template</title>
  </head>
  <body>
    <div class="lb-container">
      <!-- Header -->
      <header style="text-align: center; margin-bottom: 3rem; padding: 2rem 0;">
        <div
          style="display: flex; justify-content: flex-end; margin-bottom: 1rem;"
        >
          <button id="theme-toggle" class="lb-button lb-button-sm">
            <span id="theme-icon">☀️</span>
            <span id="theme-text">Light</span>
          </button>
        </div>
        <h1 class="lb-heading lb-heading-lg lb-mb-sm">Light Breeze UI</h1>
        <p class="lb-text lb-text-lg">
            setTimeout(() => toast.remove(), 400);
          });
        }

        setTimeout(() => {
          if (toast.parentNode) {
            toast.classList.remove("show");
            toast.classList.add("hide");
            setTimeout(() => toast.remove(), 400);
          }
        }, 4000);
      }

      document.querySelectorAll("[data-show-alert]").forEach((btn) => {
        btn.addEventListener("click", () => {
          showToast(btn.getAttribute("data-show-alert"));
        });
      });

      // Modal logic
      document.getElementById("open-modal-1").addEventListener("click", () => {
        document.getElementById("modal-1").classList.add("active");
      });

      document.getElementById("open-modal-2").addEventListener("click", () => {
        document.getElementById("modal-2").classList.add("active");
      });

      document.querySelectorAll("[data-close]").forEach((btn) => {
        btn.addEventListener("click", () => {
          const id = btn.getAttribute("data-close");
          document.getElementById(id).classList.remove("active");
        });
      });

      document.querySelectorAll(".lb-modal-overlay").forEach((overlay) => {
        overlay.addEventListener("click", (e) => {
          if (e.target === overlay) {
            overlay.classList.remove("active");
          }
        });
      });

      document.addEventListener("keydown", (e) => {
        if (e.key === "Escape") {
          document
            .querySelectorAll(".lb-modal-overlay.active")
            .forEach((m) => m.classList.remove("active"));
        }
      });
    </script>
  </body>
  <style>
    section {
      min-height: 300px;
    }
  </style>
</html>
=======
import BaseLayout from '../layouts/BaseLayout.astro';
import HeroSection from '../components/HeroSection.astro';
import ButtonsSection from '../components/sections/ButtonsSection.astro';
import CardsSection from '../components/sections/CardsSection.astro';
import FormsSection from '../components/sections/FormsSection.astro';
import BadgesSection from '../components/sections/BadgesSection.astro';
import AlertsSection from '../components/sections/AlertsSection.astro';
import ModalsSection from '../components/sections/ModalsSection.astro';
import UsageSection from '../components/sections/UsageSection.astro';
---

<BaseLayout title="Light Breeze UI - Frutiger Aero CSS Template">
  <HeroSection />
  <ButtonsSection />
  <CardsSection />
  <FormsSection />
  <BadgesSection />
  <AlertsSection />
  <ModalsSection />
  <UsageSection />
</BaseLayout>
